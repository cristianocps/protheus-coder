# MCP server framework (Streamable HTTP transport, bundles Starlette + Uvicorn).
fastmcp>=2.9,<4

# Claude Agent SDK (formerly claude-code-sdk). Drives the Claude Code agent loop
# headlessly; requires the @anthropic-ai/claude-code CLI on PATH.
claude-agent-sdk>=0.1,<1
